#include<bits/stdc++.h> 
using namespace std;

int main()
{
    cout<<"В ведите случайное число:";
    int r1;
    cin >> r1;
    cout<<"Игра началась "<<endl;
    cout<<"Вы находитесь в просторном кобинете с единственной массивной дверью  ";
    cout<< "Вы абсолютно не помните как вы здсь аказлисью Вокруг довольно светло ";
    cout<< "хотя и вы не видите источника света. Массивная дубовая дверь будто бы ";
    cout<< "притягивает вас к себе вы готовы паклясца что за ней ктото или чтото шумит."<<endl;
    cout<< "Что вы зделаете  <1>чтобы постораться открыть дверь <2>осмотреть комноту"<<endl;
    int r,a1,a12,a2,a3;
   
    cin >> a1;
    const int N=10;
    string A;
    
    srand(r1);
    
    for (int i = 0; i < a1; i++){
    }
    for (int i = 0 ; i < 1; i++){
        if(a1==1){
            r1+=1;
            cout<<"Дверь с лиденящем скржитом распахивается вы входите неё"<<endl;
        }
    }
    for (int i = 0 ; i < 1  ; i++){
        if(a1==2 ){
            r1+=2;
            cout<<"Осмотревшись на столе вы замечает старинный ключ с гровеовкой в ввиде ворона "<<endl;
            A+="ключ с гровеовкой в ввиде ворона";
            r = rand()%10;
            
            for (int i = 0 ; i < 1  ; i++){
                if(r>7 ){
                    cout<<"в яшике стола револвер с 3 потронами в нутри   "<<endl;
                }
            }
                    
            for (int i = 0 ; i < 1  ; i++){
                if(r>4 and r<7){
                    cout<<" в углу на кушетки фонарь  "<<endl;
                }
            }
                    
        cout<< "Что вы зделаете  <1>чтобы постораться открыть дверь"<<endl;
            cin >> a12;
            for (int i = 0 ; i < 1; i++){
                if(a12==1){
                    cout<<"вы пытаетесь вставить Дверь с лиденящем скржитом распахивается вы входите неё"<<endl;
                
                }
            }
        }
    }
    cout<< "Перед тобой открываетс узенький хол обшарпаный и грязный пряио перед тобой большая железная дверь с 3 значнымзмком  "<<endl;
    cout<< "в вашем инвенторе"<<endl;
    cout<< A;
    cin >> a2;
    for (int i = 0 ; i < 1  ; i++){
        if(a2==1 ){
            cout<<"Дверь";
        }
    }
    for (int i = 0 ; i < 1  ; i++){
        if(a2==2 ){
            cout<<"Падвал";
        }
    }
    for (int i = 0 ; i < 1  ; i++){
        if(a2==3 ){
            cout<<"Дверь 2";
        }
    }
    for (int i = 0 ; i < 1  ; i++){
        if(a2==4 ){
            cout<<"Зайти";
            cin >> a3;
            for (int i = 0 ; i < 1  ; i++){
                if(a3==1 ){
                    cout<<"Дверь";
                }
            }
            for (int i = 0 ; i < 1  ; i++){
                if(a3==2 ){
                    cout<<"Падвал";
                }
            }
            for (int i = 0 ; i < 1  ; i++){
                if(a3==3 ){
                    cout<<"Дверь 2";
                }
            }
    
        }
    }
    
    return 0;
}

